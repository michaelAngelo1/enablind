import 'package:flutter/material.dart';

class PastJobsPage extends StatelessWidget {
  const PastJobsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Past Jobs Page'),
    );
  }
}